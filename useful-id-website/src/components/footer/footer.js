import './footer.css';

function Footer() {
    return (
        <footer className="footer-section">
            <span>This should contain stuff that goes in a footer</span>
        </footer>
    );
}

export default Footer;
