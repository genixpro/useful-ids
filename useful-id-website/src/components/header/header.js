import './header.css';

function Header() {
    return (
        <header className="header-section">
            <h1>Useful Ids</h1>
            <span>A manifesto to make IDs in software useful</span>
        </header>
    );
}

export default Header;
